spring:
  application:
    name: bidstream
  datasource:
    url: jdbc:postgresql://localhost:5433/bidstream
    username: bidstream
    password: bidstream
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration
